import React from 'react';
import style from './Header.module.css';
const Header = () => {
    return (
        <div>
            <h1 className={style.title}>This is Header</h1>
        </div>
    );
};

export default Header;